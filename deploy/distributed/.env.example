# Database connection string - CHANGE IN PRODUCTION
POSTGRES_CONNECTION_STRING=Host=database;Database=openmu;Username=postgres;Password=changeme

# MinIO credentials - CHANGE IN PRODUCTION
MINIO_ACCESS_KEY=loki
MINIO_SECRET_KEY=changeme

# Redis password - CHANGE IN PRODUCTION
REDIS_PASSWORD=changeme

# Domain for production SSL
DOMAIN_NAME=your-domain.com
