apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: login-state
spec:
  spec:
  type: state.redis
  version: v1
  metadata:
  - name: redisHost
    value: redis-state:6379
  - name: redisPassword
    value: "${REDIS_PASSWORD:-openmu-dev}"
  #- name: ttlInSeconds
  #  value: <int> # Optional